import React from "react";

/**
 * Page qui montre la liste des activités validées, et qui permet de les lancer
 */
export function ActivityLauncher() {
    return (
        <h1>{__filename}</h1>
    )
}

/**
 * Page qui montre la liste des activités terminées, et qui permet de voir les évaluations des élèves
 */
export function ActivityNotes() {
    return (
        <h1>{__filename}</h1>
    )
}

/**
 * Liste des activités terminées, d'où les accompagnateurs pourront évaluer leurs performances etc
 */
export function ActivityDone() {
    return (
        <h1>{__filename}</h1>
    )
}